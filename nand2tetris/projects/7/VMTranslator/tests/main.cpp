#include <iostream>
#include <filesystem>
#include <fstream>
#include <string>

class fileTest {
private:


public:
    fileTest() = default;
    
};

auto main() -> int {

    // Check if dir
    // read sub files and parse
    //
    // write to all parsed to 1 file

}
