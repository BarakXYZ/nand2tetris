class Main {

    function void main() {
        
        var Game game;
        let game = Game.new();

        // INIT
        do NPC.init();
        do QuestManager.init();
        do Sprites.init();
        do Draw.spriteDirect();  // Preview Sprite fast
        do Lookup.init();
        do Challenge.init();
        do Draw.init();
        do UserInterface.init();
        do Debug.init();
        // do Debug.run();

        do game.run();

        return;
    }
}
